@page "/server"


@using Microsoft.JSInterop
@using upgraded_spork.Client.Components

<PageTitle>Control Panel</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">Server UI Mockup</MudText>

@*
    Things to note:
    Each server page will need a settings page with all of the configuration for each server
    On the main page, we need to be able to view information on the running proces. 
    Such as:
     - stdout
     - Status
    We need to be able to perform the following interactions:
     - Start
     - Stop
     - Restart
     - Status (poll, both for status on linux and for proc.poll)
     - Send a command via stdin
*@

@*Container to template*@
<MudPaper Elevation="3" Style="padding: 10px;">
    <MudTextField 
        T="string" 
        Label="stdout" 
        Variant="Variant.Outlined" 
        Text="@sampleText" 
        Lines="25" 
        ValueChanged="TextChanged"
    />

    <textarea 
        id="logTextArea" 
        style="width:600px;height:200px;"
        onchange="">
        @sampleText
    </textarea>

    <ProcessControlButtonGroup />
    
    <br/>

    <MudButton OnClick="ShowAlertWindow">multiply Text in window</MudButton>
</MudPaper>